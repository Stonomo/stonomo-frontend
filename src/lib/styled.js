import { Typography, styled } from '@mui/material'

export const dayjsDateFormat = 'MMM-DD-YYYY'

export const Label = styled(Typography)(() => ({
	fontWeight: 'bold'
}))

export const Content = styled(Typography)(() => ({
	marginLeft: '5px'
}))